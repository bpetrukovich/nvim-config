return {
  -- 'michaelb/sniprun',
  -- branch = 'master',
  --
  -- build = 'sh install.sh',
  -- -- do 'sh install.sh 1' if you want to force compile locally
  -- -- (instead of fetching a binary from the github release). Requires Rust >= 1.65
  --
  -- config = function()
  --   require('sniprun').setup {}
  --
  --   vim.keymap.set('v', '<leader>rc', '<cmd>SnipRun<CR>', { silent = true })
  --   vim.keymap.set('n', '<leader>rc', '<cmd>SnipRun<CR>', { silent = true })
  --   vim.keymap.set('n', '<leader>ro', '<cmd>SnipRunOperator<CR>', { silent = true })
  -- end,
}
